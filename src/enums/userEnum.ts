export enum RoleEnum {
  SUPER = 'super',
  VISITOR = 'visitor'
}

export enum LoginResultEnum {
  SUCCESS = 'success',
  FAILED = 'failed'
}

export enum SexEnum {
  FEMALE = 'female',
  MALE = 'male'
}
